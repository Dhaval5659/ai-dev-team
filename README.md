> install .venv
python -m venv .venv
> activate .venc
.venv\Scripts\activate
> dotenc is missing
pip install python-dotenv
> crewai model is missing
pip install crewai crewai-tools

> install all modul
pip install crewai crewai-tools langchain-openai python-dotenv pyyaml chromadb

> to run 
python src/ai_dev_team/main.py
